# concurrency

Concurrency and the C++ memory model: atomics, acquire-release semantics, sequential consistency, thread synchronization primitives, and data race analysis.

Each subdirectory corresponds to one LinkedIn post.
