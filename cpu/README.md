# cpu

CPU-level behavior: cache hierarchies, false sharing, branch prediction, pipeline stalls, out-of-order execution, and hardware prefetching.

Each subdirectory corresponds to one LinkedIn post.
