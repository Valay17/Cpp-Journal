# intrinsics

SIMD and manual vectorization: SSE, AVX, AVX-512 intrinsics, auto-vectorization hints, and measuring throughput vs latency of vector instructions.

Each subdirectory corresponds to one LinkedIn post.
